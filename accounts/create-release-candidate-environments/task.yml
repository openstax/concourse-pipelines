---
platform: linux

inputs:
- name: accounts-git
- name: concourse-pipelines
- name: accounts-ami

params:
  AWS_SECRET: ((aws-sandbox-secret-access-key))
  AWS_KEY: ((aws-sandbox-secret-key-id))
  
run:
  path: concourse-pipelines/accounts/create-release-candidate-environments/create-release-candidate-environments.sh
